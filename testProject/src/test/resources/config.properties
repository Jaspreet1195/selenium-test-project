browser=chrome
headless=true
baseURL=https://webdriveruniversity.com/index.html
username=jaspreet
password=jaspreet
# Use in-memory H2 for integration tests; avoids needing a local MySQL instance.
db.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
db.user=sa
db.password=
db.driver=org.h2.Driver

apiBaseURL=https://demo.duendesoftware.com
tokenEndPoint=/connect/token
apiUsername=m2m
apiPassword=secret
applicationEndPoint=/api/test
selenium.grid.url=

# Supply real secrets via env vars or -D overrides
OPENAI_KEY=
